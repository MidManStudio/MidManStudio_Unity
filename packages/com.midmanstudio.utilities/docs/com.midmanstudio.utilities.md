# com.midmanstudio.utilities

## Overview

Shared runtime and editor utility library used across MidManStudio packages
and projects: singletons, logging, event bus, object/particle pooling,
scriptable-object libraries, scene management, UI state, FX playback, audio
playback, auto-reference wiring, hierarchy tooling, timers, and small editor
windows for each of those systems. 126 files across `Runtime/` and `Editor/`,
plus `Samples~/` (optional example scenes/scripts, not part of the shipped
package) and `Tests/` (manual benchmark runners, not an automated suite).

This package is too large for a single doc file (see the Crate and File
Structure rule in the documentation guidelines), so it is split by part,
matching the existing `Runtime/<Subsystem>` / `Editor/<Subsystem>` folder
grouping. Auto-generated files (anything under a `Generated/` folder, each
already marked `AUTO-GENERATED — do not edit`) are out of scope for this
documentation effort; they are regenerated by their editor tool, not
hand-maintained.

## Parts

- [Singleton](com.midmanstudio.utilities/singleton.md) - `Singleton<T>`
  (MonoBehaviour singleton) and `StaticContentSingleton<T>` (plain C# class
  singleton). Done.
- Logging - not yet covered.
- Events - not yet covered.
- SequentialProcessRunner - not yet covered.
- HierarchyArranger - not yet covered.
- Timers - not yet covered.
- TickDispatcher - not yet covered.
- StickyNote - not yet covered.
- Libraries - not yet covered.
- SceneManagement - not yet covered.
- AutoReference - not yet covered.
- Audio - not yet covered.
- FXSystems - not yet covered.
- UIState - not yet covered.
- PoolSystems - not yet covered.
- Helpers - not yet covered.
- Ui - not yet covered.
- ScriptableObjectViewer (`Editor/ScriptableObjectViewer`) - not yet covered.
- `Runtime/Editor/DynamicDebugPanel.cs` - not yet covered; doesn't fit an
  existing part, needs its own small part or a home in Helpers.

## CI and Workflows

- `.github/workflows/release-packages.yml` - tags and releases this package
  (`com.midmanstudio.utilities`) from `packages/com.midmanstudio.utilities`
  on a `utilities/v*` tag or manual dispatch.
- `.github/workflows/build-audio-libs.yml` - builds the native `mid_audio`
  DSP library used by the Audio part, for all supported Unity platforms.
  Builds clean every run, no caching, by design (see the Audio part's Fixes
  and Problems once that part is written).
