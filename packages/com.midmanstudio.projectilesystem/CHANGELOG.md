# com.midmanstudio.projectilesystem — Package Catalog
**MidMan Projectile System** v1.1.0 | Unity 2022.3+  
Last updated: 2026-06-17

> **2D PRIMARY:** The 2D simulation path (`Is3D = false`) is the fully tested, production-ready path.  
> **3D REFERENCE:** The 3D path (`Is3D = true`) is a functional reference implementation — it runs correctly but has seen less real-world testing. Rendering quality on lower-end or integrated GPUs may vary due to billboard instancing overhead.

> ⚠ **Discrepancy fixes vs. old placeholder CHANGELOG are marked with ⚠ FIX.**

---

## Full Folder Tree

```
com.midmanstudio.projectilesystem/
│
├── package.json                                       ← ⚠ FIX: add netcode dep + fix versions (see below)
├── CHANGELOG.md
├── LICENSE.md
├── README.md
├── APICATALOG.md
│
├── Runtime/                                           ← MidManStudio.ProjectileSystem.asmdef
│   ├── MidManStudio.ProjectileSystem.asmdef           ← allowUnsafeCode: true; autoReferenced: true
│   │
│   ├── Adapters/
│   │   ├── BatchSpawnHelper.cs                        ← Low-level 2D/3D batch spawn; Burst IJob paths;
│   │   │                                                 FIX: Wave/Circular Ax/Ay = perp of fire dir;
│   │   │                                                 FIX: latency comp reduces Lifetime only (no pos advance)
│   │   ├── ProjectileTypeRouter.cs                    ← Routes WeaponFireContext → SimulationMode;
│   │   │                                                 per-config override; ComputeMode(); ExplainRoute()
│   │   └── RustSimAdapter.cs                          ← 2D/3D hit processing; HandlePiercing;
│   │                                                     FIX: Unregister() now called so Alive clears correctly
│   │
│   ├── Config/
│   │   ├── Generator/
│   │   │   ├── ProjectileConfigEntry.cs               ← One named enum entry (enumName, configSO, pin offset)
│   │   │   ├── ProjectileConfigGeneratorSettingsSO.cs ← Generator output paths, block size, namespace
│   │   │   └── ProjectileConfigProviderSO.cs          ← Per-package config type list SO
│   │   ├── MathFormulaEvaluator.cs                   ← Self-contained recursive-descent math parser;
│   │   │                                                 used by Pattern/Shape Formula presets
│   │   ├── ProjectileConfigManager.cs                 ← Runtime bridge: enum int → ushort configId;
│   │   │                                                 extension method system.Fire(ProjectileConfigType)
│   │   ├── ProjectileConfigMappingSO.cs               ← Ordered array: index == ProjectileConfigType int value;
│   │   │                                                 auto-generated by Config Type Generator
│   │   ├── ProjectileConfigSO.cs                      ← Core config asset: movement, damage curve, visual,
│   │   │                                                 trail, piercing, scale growth, wave/circular params;
│   │   │                                                 FIX: RustSim2D + RustSim3D merged → RustSim (Is3D decides buffer)
│   │   │                                                 FIX: _hitLayers (LayerMask) added
│   │   │                                                 FIX: scale ScaleStart now uses _fullSizeX when !UseScaleGrowth
│   │   │                                                 ADDED: public Wave/Circular param accessors for DeterministicMotionMath
│   │   ├── ProjectilePatternSO.cs                     ← Shot pattern SO: Spline/Ring360/Fan/VShape/Shotgun/
│   │   │                                                 Star/Spiral/Formula; SampleDirections() returns (H°,V°) pairs
│   │   │                                                 ADDED: PatternShape.Formula + _patternFormulaH/V fields
│   │   ├── ProjectileRegistry.cs                      ← Runtime configId registry; auto-registers on Awake;
│   │   │                                                 ValidateStructSizes() at startup; GetUVRect() for atlas
│   │   └── ProjectileShapeSO.cs                       ← Visual shape SO: Quad/Needle/Diamond/Arrow/Cross/
│   │                                                     Chevron/Star4/Boomerang/LetterI/T/L/Custom/Formula;
│   │                                                     ADDED: Preset.Formula + BuildFormula() parametric mesh
│   │
│   ├── Core/
│   │   ├── MID_BuildTargets.cs                        ← IsServerBuild / IsClientBuild / IsEditor constants
│   │   ├── NativeProjectile.cs                        ← 72-byte 2D sim struct (LayoutKind.Explicit)
│   │   │                                                 ⚠ PRIMARY — this is the main sim struct
│   │   ├── ProjectileLib.cs                           ← ALL P/Invoke bindings; ValidateStructSizes();
│   │   │                                                 FetchMovementTypeConstants(); platform DLL routing
│   │   ├── ProjectileManager.cs                       ← Marker MonoBehaviour (tag component only)
│   │   ├── RustSpawnParams.cs                         ← Minimal spawn params struct (no Unity objects)
│   │   └── SimulationMode.cs                          ← Raycast=0 / RustSim=1 / PhysicsObject=3 / LocalOnly=4;
│   │                                                     FIX: RustSim2D(1) + RustSim3D(2) merged → RustSim(1)
│   │
│   ├── Data/
│   │   ├── MID_AllProjectileNames.cs                  ← Legacy enum stub (Default only); only kept for
│   │   │                                                 INetworkProjectileVisual.Initialize() signature compat
│   │   └── ServerProjectileData.cs                    ← Server-side gameplay data per projectile;
│   │                                                     CloneForSpawn() / CloneForSpawn3D() for batch spawns
│   │
│   ├── Managers/
│   │   ├── LocalProjectileManager.cs                  ← Offline + client-side Rust buffer; handles:
│   │   │                                                 offline spawns, firing-client temp ID visual,
│   │   │                                                 other-client network spawns, snapshot reconciliation;
│   │   │                                                 FIX: ReconcileSnapshots extrapolates forward → no zig-zag
│   │   │                                                 FIX: per-projectile pierce immunity dict (not per-tick)
│   │   ├── MID_MasterProjectileSystem.cs              ← PRIMARY GAME CODE ENTRY POINT (Singleton);
│   │   │                                                 Fire() / RegisterRaycastFire() / RegisterTarget2D/3D();
│   │   │                                                 FIX: firing client spawns into LocalProjectileManager immediately
│   │   │                                                 FIX: targets no longer registered in LocalMgr when networked
│   │   ├── PhysicsProjectile2D.cs                     ← Rigidbody2D physics projectile (2D PRIMARY)
│   │   ├── PhysicsProjectile3D.cs                     ← Rigidbody physics projectile (3D REFERENCE)
│   │   │                                                 FIX: GravityScale now read from config (not just inspector)
│   │   ├── PhysicsProjectileBase.cs                   ← Shared physics projectile logic (NetworkTransform base);
│   │   │                                                 FIX: damage now uses config damage curve + travel dist
│   │   │                                                 FIX: _spawnPosition tracked for damage falloff
│   │   │                                                 FIX: ShouldAutoSpawnVisual prevents double-visual
│   │   ├── RaycastProjectileHandler.cs                ← Server hitscan validation + client travelling visual;
│   │   │                                                 FIX: firing client excluded from SpawnVisualClientRpc
│   │   │                                                 FIX: 2D uses ContactFilter2D (triggers always included)
│   │   │                                                 FIX: _trustClientOnValidationMiss fallback added
│   │   └── ServerProjectileAuthority.cs               ← Server-only Rust 2D/3D buffer owner; FixedUpdate sim loop;
│   │                                                     FIX: _renderer2D/_renderer3D added for host rendering
│   │                                                     FIX: snapshots now skip Wave/Circular (bandwidth + queue fix)
│   │
│   ├── Network/
│   │   ├── ClientPredictionManager.cs                 ← Physics pool visual manager (simplified);
│   │   │                                                 SpawnLocalPhysicsVisual() / KillPhysicsVisual();
│   │   │                                                 static rotation utils used by raycast + physics paths
│   │   ├── ClientProjectileVisualManager.cs           ← Stub for other-player visuals (implement in game assembly)
│   │   ├── DeterministicMotionMath.cs                 ← Closed-form Wave/Circular 2D+3D position formulas;
│   │   │                                                 must match Rust simulation.rs tick equations exactly
│   │   ├── INetworkProjectileVisual.cs                ← Interface for physics NetworkObject visuals
│   │   ├── MID_ProjectileNetworkBridge.cs             ← All NGO RPCs: FireServerRpc, SpawnConfirmedClientRpc,
│   │   │                                                 LinkProjectileIdsClientRpc, HitConfirmedClientRpc,
│   │   │                                                 SendSnapshotClientRpc;
│   │   │                                                 FIX: firing client gets Link RPC, others get Spawn RPC
│   │   │                                                 FIX: snapshot passes tick+interval for extrapolation
│   │   └── NetworkProjectileBase.cs                   ← NetworkTransform base; InitialiseProjectile(); pool visual;
│   │                                                     FIX: ShouldAutoSpawnVisual virtual bool prevents double-visual
│   │                                                     FIX: OnNetworkVelocityReceived() for client visual init
│   │
│   ├── Plugins/
│   │   └── Native/                                    ← Compiled Rust projectile_core library
│   │       ├── Windows/projectile_core.dll
│   │       ├── macOS/projectile_core.bundle
│   │       ├── Android/libprojectile_core.so
│   │       ├── Linux/libprojectile_core.so
│   │       └── iOS + WebGL: libprojectile_core.a      ← "__Internal" — static link only
│   │
│   ├── Shaders/
│   │   ├── InstancedProjectile.shader                 ← Built-in RP; FIX: float4 color:COLOR for combined-mesh fade
│   │   └── InstancedProjectile_URP.shader             ← URP; FIX: #ifdef UNITY_INSTANCING_ENABLED branches
│   │                                                     prevent material default _UVRect corrupting combined-mesh UVs
│   │
│   └── Visual/
│       ├── ProjectileImpactHandler.cs                 ← GlobalFXManager integration (primary) + strategy fallback;
│       │                                                 per-config EffectType bindings; headshotEffectType
│       ├── ProjectileRenderer2D.cs                    ← ⚠ PRIMARY RENDERER — GPU DrawMeshInstanced + combined-mesh
│       │                                                 fallback; per-configId grouping; atlas UV support;
│       │                                                 FIX: MAX_SHAPE_VERTS 7→12, MAX_SHAPE_TRIS 15→30 (Cross/LetterI)
│       │                                                 FIX: dynamic vBase/tBase tracking for non-quad shapes
│       ├── ProjectileRenderer3D.cs                    ← 3D billboard renderer (REFERENCE); two separate mesh objects;
│       │                                                 FIX: separate _spriteMesh/_shapeMesh (was shared → GPU clobber)
│       ├── ProjectileVisual3D.cs                      ← 3D pool visual (REFERENCE); MeshFilter + TrailRenderer;
│       │                                                 FIX: _trailRenderer now auto-found in Awake if unassigned
│       ├── ProjectileVisualBase.cs                    ← Abstract MonoBehaviour base for all pooled visuals;
│       │                                                 InitializeClientVisual() / ReturnToPoolImmediate()
│       ├── ProjectileVisual_.cs                       ← ⚠ PRIMARY 2D pool visual; SpriteRenderer + TrailRenderer;
│       │                                                 FIX: MeshFilter/MeshRenderer created dynamically at runtime
│       │                                                 for CustomShape configs (prefab no longer needs them pre-added)
│       └── TrailObjectPool.cs                         ← Trail slot manager; syncs positions from Rust buffer;
│                                                         ADDED: SyncToSimulation3D() for 3D buffer
│
├── Editor/                                            ← MidManStudio.ProjectileSystem.Editor.asmdef
│   ├── MidManStudio.ProjectileSystem.Editor.asmdef    ← ⚠ FIX: add Unity.Netcode.Runtime (was only transitive)
│   ├── ProjectileConfigBootstrapper.cs                ← [InitializeOnLoad] auto-creates DefaultProjectile config,
│   │                                                     provider, and generator settings on first import
│   ├── ProjectileConfigGenerator.cs                   ← Config Type Generator window; writes ProjectileConfigType.cs
│   │                                                     + ProjectileConfigMapping.asset; lock-file backed;
│   │                                                     FIX: [pinned] tag always inside // comment (was invalid C#)
│   ├── ProjectilePatternEditor.cs                     ← Custom inspector for ProjectilePatternSO; interactive
│   │                                                     preview viewport; spline control-point dragging;
│   │                                                     ADDED: Formula shape validation + example dropdown
│   ├── ProjectileShapeEditor.cs                       ← Custom inspector for ProjectileShapeSO; canvas editor;
│   │                                                     ADDED: Formula shape validation + live mesh rebuild
│   └── ProjectileSystemPoolProviderBootstrapper.cs    ← [InitializeOnLoad] auto-creates object/particle/network
│                                                         pool provider SOs on first import
│
└── Tests/Runtime/                                     ← Test/example scene scripts (NOT package API)
    ├── DimensionManager.cs                            ← 2D↔3D switcher; configurable toggle key
    ├── NetworkedDimensionPlayer.cs                    ← Networked dual-dimension player script
    ├── ProjectileTestLobbyUI.cs                       ← Extends LocalLobbyUIManager for test scene
    └── TestSceneBootstrapper.cs                       ← Test scene setup; config Path A/B registration
```

---

## Discrepancy Fix Summary

| # | Location | Issue | Fix |
|---|---|---|---|
| 1 | `package.json` | `com.midmanstudio.netcode` referenced in asmdef but missing from package dependencies | Added `com.midmanstudio.netcode: 1.0.0` |
| 2 | `package.json` | `com.unity.collections: 2.2.0` — should match utilities | Changed to `2.2.1` |
| 3 | `package.json` | `com.unity.mathematics: 1.2.6` — should match utilities | Changed to `1.3.1` |
| 4 | Editor asmdef | `Unity.Netcode.Runtime` only arrived transitively via `MidManStudio.Netcode` | Added explicitly |
| 5 | Old CHANGELOG | Placeholder content (wrong systems listed) | Replaced with correct projectile content |
| 6 | `SimulationMode.cs` | `RustSim2D` (1) and `RustSim3D` (2) were separate values | Merged to `RustSim` (1); `Is3D` on config selects buffer |
| 7 | `ProjectileConfigSO` | `_preferredSimMode` default was `RustSim2D`; `HasSimModeOverride` compared against it | Updated default to `RustSim` |
| 8 | `BatchSpawnHelper` | Wave/Circular Ax/Ay was `(0, gravityAy)` — wrong axis for oscillation | Changed to `(-dir.y, dir.x)` perpendicular |
| 9 | `BatchSpawnHelper` | Latency comp advanced X/Y position — host saw bullets spawn ahead of barrel | Position no longer advanced; only Lifetime reduced |
| 10 | `RustSimAdapter.HandlePiercing` | Set `hasHit=true` but never called `Unregister()` → projectiles lived forever | `Unregister()` + `OnProjectileDied` now always called on kill |
| 11 | `LocalProjectileManager.ReconcileSnapshots` | Compared stale snapshot against current position → zig-zag | Extrapolates snapshot forward by `staleTime` before comparing |
| 12 | `ServerProjectileAuthority` | No `_renderer2D/_renderer3D` — host saw no projectiles | Added renderers + `LateUpdate()` call |
| 13 | `ServerProjectileAuthority` | Wave/Circular snapshots sent to clients (useless + filled UTP receive queue) | Filtered out in `SendSnapshots()` |
| 14 | `RaycastProjectileHandler` | Firing client received `SpawnVisualClientRpc` despite already having a local visual | Sender excluded; gets `LinkProjectileIdsClientRpc` instead |
| 15 | `RaycastProjectileHandler` | 2D server validation used `Physics2D.Raycast` overload that respects `queriesHitTriggers` setting | Changed to `ContactFilter2D` with `useTriggers = true` |
| 16 | `PhysicsProjectileBase` | Damage always used `_baseDamage` — config damage curve never evaluated | Now calls `cfg.EvaluateDamage(normDist)` using travel distance |
| 17 | `PhysicsProjectile3D` | `_useGravity` was hardcoded inspector field — no config hookup | Now reads `cfg.GravityScale` when config is registered |
| 18 | `NetworkProjectileBase` | `OnNetworkSpawn` spawned a pool visual AND `PhysicsProjectile.SpawnPoolVisual` spawned another | Added `ShouldAutoSpawnVisual` virtual bool; physics overrides `false` |
| 19 | `ProjectileRenderer2D` combined-mesh | Used fixed `qi*4` / `qi*6` stride — corrupted non-quad shapes (Arrow, Cross etc.) | Now tracks `vBase` / `tBase` dynamically per shape |
| 20 | `ProjectileRenderer3D` | Both passes shared one mesh — pass 2 clobbered pass 1 geometry | Now uses `_spriteMesh` and `_shapeMesh` as separate objects |
| 21 | `InstancedProjectile_URP.shader` | No `#ifdef UNITY_INSTANCING_ENABLED` — combined-mesh path read material default `_UVRect` which corrupted UVs | Added `#ifdef` branches; combined-mesh uses pre-baked vertex UVs |
| 22 | `ProjectileVisual_.cs` | CustomShape pool prefabs required pre-added `MeshFilter/MeshRenderer` | Components now created dynamically at runtime on first shape use |
| 23 | `ProjectileVisual3D.cs` | `_trailRenderer` had no auto-find fallback — silently null → no trail | Added `GetComponentInChildren<TrailRenderer>(true)` in Awake |
| 24 | `ProjectilePatternSO` / `ProjectileShapeSO` | No formula-driven shot patterns or shapes | Added `PatternShape.Formula` and `Preset.Formula` with `MathFormulaEvaluator` |
| 25 | `ProjectileConfigGenerator` | `[pinned]` tag appended without `//` prefix when Comment empty → invalid C# | Always emitted inside `//` comment |

---

## Namespace Map

| Folder | Namespace |
|---|---|
| `Core/` | `MidManStudio.Projectiles.Core` |
| `Config/` | `MidManStudio.Projectiles.Config` |
| `Config/Generator/` | `MidManStudio.Projectiles.Config` |
| `Adapters/` | `MidManStudio.Projectiles.Adapters` |
| `Managers/` | `MidManStudio.Projectiles.Managers` |
| `Network/` | `MidManStudio.Projectiles.Network` |
| `Visual/` | `MidManStudio.Projectiles.Visuals` |
| `Data/` | `MidManStudio.Projectiles.Data` |
| `Editor/` | `MidManStudio.Projectiles.EditorUtils` / `MidManStudio.Projectiles.EditorTools` |
| `Tests/Runtime/` | `TestGame` (example scripts — not package API) |

---

## Assembly Structure

```
MidManStudio.ProjectileSystem         autoReferenced: true  | allowUnsafeCode: true
├── MidManStudio.Utilities
├── MidManStudio.Netcode
├── Unity.Netcode.Runtime
├── Unity.Netcode.Components
├── Unity.Burst
├── Unity.Collections
└── Unity.Mathematics

MidManStudio.ProjectileSystem.Editor  autoReferenced: false | Editor only
├── MidManStudio.ProjectileSystem
├── MidManStudio.Utilities
├── MidManStudio.Utilities.Editor
├── MidManStudio.Netcode
└── Unity.Netcode.Runtime
```

---

## Pool Type Blocks (registered by ProjectileSystemPoolProviderBootstrapper)

**Object pool** (`priority = 10`, block 100–199):

| Offset | Name | Description |
|---|---|---|
| 100 | `Projectile_Visual2D` | 2D sprite + trail pool visual |
| 101 | `Projectile_Visual3D` | 3D mesh + trail pool visual (reference) |
| 102 | `Projectile_Flipbook` | Sprite-sheet impact flipbook |

**Particle pool** (`priority = 10`, block 100–199):

| Offset | Name | Description |
|---|---|---|
| 100 | `Projectile_Impact` | Generic hit / impact particle |
| 101 | `Projectile_Explosion_Small` | Small explosion |
| 102 | `Projectile_Explosion_Medium` | Medium explosion |
| 103 | `Projectile_Explosion_Large` | Large explosion |
| 104 | `Projectile_Headshot` | Headshot / critical hit particle |
| 105 | `Projectile_Tracer` | Tracer round particle |

**Network object pool** (`priority = 10`, block 100–199):

| Offset | Name | Description |
|---|---|---|
| 100 | `BaseProjectileBlueprint_2D` | 2D physics projectile NetworkObject |
| 101 | `BaseProjectileBlueprint_3D` | 3D physics projectile NetworkObject (reference) |

---

## [1.1.0] — Unreleased

### Added
- `ProjectileConfigSO._hitLayers` — `LayerMask` controlling which Unity layers a projectile can register hits against
- `ProjectileConfigSO` public accessors for wave/circular movement parameters (`WaveAmplitude`, `WaveFrequency`, `WavePhaseOffset`, `WaveVertical`, `CircularRadius`, `CircularAngularSpeed`, `CircularStartAngle`) so `DeterministicMotionMath` can read them without reflection
- `ProjectileConfigManager` + `ProjectileConfigMappingSO` — enum-based config resolution system (`ProjectileConfigType.MyBullet` → `ushort configId`)
- `ProjectileConfigTypeExtensions.Fire()` — extension method allowing `system.Fire((int)ProjectileConfigType.X, pts, n, ctx)` directly
- `ProjectileConfigGeneratorSettingsSO` + `ProjectileConfigProviderSO` + `ProjectileConfigEntry` — SO-driven Config Type Generator
- `ProjectileConfigGenerator` editor window — writes `ProjectileConfigType.cs` + updates `ProjectileConfigMappingSO`; lock-file backed
- `ProjectileConfigBootstrapper` — auto-creates default config assets on first package import
- `PatternShape.Formula` in `ProjectilePatternSO` — H(i,n) and V(i,n) math expressions for per-projectile shot angles
- `Preset.Formula` in `ProjectileShapeSO` — parametric X(t) / Y(t) mesh generation; center-fan triangulation
- `MathFormulaEvaluator` — self-contained recursive-descent expression parser with 30+ math functions and built-in example presets
- `ProjectilePatternEditor` / `ProjectileShapeEditor` — custom inspectors with live formula validation, example dropdowns, interactive viewport
- `DeterministicMotionMath` — closed-form 2D/3D position + velocity formulas for Wave and Circular types; perpendicular axis helpers
- `TrailObjectPool.SyncToSimulation3D()` — syncs 3D Rust buffer positions to trail slots
- `ProjectileImpactHandler` — `GlobalFXManager` integration path; per-config `EffectType` bindings; headshot override
- `ServerProjectileAuthority._renderer2D/_renderer3D` + `LateUpdate()` — host now renders from server buffer
- `ClientPredictionManager.SpawnLocalPhysicsVisual()` / `KillPhysicsVisual()` — explicit physics pool visual lifecycle
- `RaycastProjectileHandler._trustClientOnValidationMiss` — configurable fallback when server re-validation raycast misses desynced targets
- `ProjectileVisual_.EnsureShapeMeshComponents()` — runtime creation of `MeshFilter/MeshRenderer` for shape configs (prefabs no longer need them pre-added)

### Fixed (see Discrepancy Fix Summary above for full list)
- Zig-zag / oscillating projectiles on clients (snapshot reconciliation extrapolation)
- Projectiles living forever despite `PiercingType.None` (`RustSimAdapter.HandlePiercing` unregister fix)
- Host seeing no projectiles (`ServerProjectileAuthority` renderer + `LateUpdate` added)
- UTP receive queue overflow from Wave/Circular snapshot spam (filtered from `SendSnapshots`)
- Firing client double-visual for Rust sim and raycast paths
- 2D raycast missing trigger colliders on server re-validation
- Physics projectile damage ignoring config damage curve
- GPU geometry corruption for non-quad shapes in combined-mesh path
- Combined-mesh path UV corruption in URP from material default `_UVRect`
- Missing trail on 3D pool visuals when `TrailRenderer` not wired in inspector
- Invalid C# from `[pinned]` tag without `//` prefix in generator output
